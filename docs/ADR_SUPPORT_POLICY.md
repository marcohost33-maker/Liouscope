# ADR: Support policy after v0.5.0 — moved

This decision record has been consolidated into the numbered ADR directory and
now lives at:

- **`docs/adr/0001-python-support-policy.md`** — Scientific-Python support policy
  (SPEC 0).

This stub is retained only so existing links do not break. See
`docs/adr/README.md` for the ADR index and conventions.
