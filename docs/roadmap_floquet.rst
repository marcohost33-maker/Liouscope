Floquet roadmap
===============

This page mirrors ``ROADMAP_FLOQUET.md`` at the repository root via an
``include`` directive so the two documents cannot drift. To edit the
roadmap, update the Markdown file; the HTML rendering here picks up the
change automatically.

.. note::

   The Floquet extension is **out of scope** for v0.2.0. Its design is
   recorded for forward planning only.

.. include:: ../ROADMAP_FLOQUET.md
   :literal:
